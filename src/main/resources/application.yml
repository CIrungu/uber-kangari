spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/driver_db
    username: postgres
    password: Cleymoh2362
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true